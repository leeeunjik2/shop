# 쇼핑몰 디자인 기준

## 1. 문서 범위와 측정 기준

- 이 문서는 `references/screens/home.png`, `detail.png`, `cart.png` 세 장에서
  직접 읽은 색, 글자 크기와 굵기, 여백, 선, 모서리를 정리한다.
- 아직 화면을 구현하지 않는다. 이 문서만 디자인 기준으로 만든다.
- 세 PNG의 메타데이터는 모두 약 120dpi이고, 사용자가 캡처 당시 브라우저
  확대율이 100%였다고 확인했다.
- 운영체제의 표시 배율은 확인하지 못했다. 따라서 120dpi를 근거로 CSS px를
  역산하지 않는다.
- 아래의 모든 크기와 좌표는 PNG에서 직접 센 **캡처 픽셀**이다. CSS 값으로
  확인하지 못한 칸은 비워 둔다.
- 이 문서에서 **사용자 승인 프로젝트 적용값**이라고 표시한 값은 캡처에서
  역산한 값이 아니다. 화면 구현에 쓰도록 사용자가 별도로 승인한 CSS 값이며,
  아래의 캡처 실측 기록과 구분한다.
- 글자의 가장자리에는 ClearType 색 번짐이 있다. 색은 번진 가장자리가 아니라
  반복해서 나타나는 불투명 내부 픽셀에서 읽었다.
- 원본 사이트의 로고, 브랜드 이름, 서비스 이름, 상품 사진과 문구는 디자인
  기준에 포함하지 않는다.

## 2. 시각 방향

- 한국 쇼핑몰에서 흔히 보이는 **정보 우선의 촘촘한 화면**을 따른다.
- 흰 바탕, 얇은 회색 구분선, 작은 글자 단계, 분명한 가격 위계를 사용한다.
- 상품 목록은 각각을 둥근 카드로 감싸지 않고 한 그리드 안에 평평하게 놓는다.
- 장바구니처럼 영역 구분이 꼭 필요한 곳만 얇은 외곽선과 작은 모서리를 쓴다.
- 큰 둥근 카드, 과한 그림자, 장식용 그라데이션, 유리 효과, 불필요한 pill을
  사용하지 않는다.
- 빈자리를 장식이나 가짜 정보로 채우지 않는다.
- 금액은 천 단위 쉼표 뒤에 `원`을 붙인다. 큰 가격에서는 숫자를 굵게 하고
  `원`은 한 단계 작게 보인다.
- 상품명 길이와 이미지 비율은 실제 데이터를 그대로 받아들인다. 같은 길이의
  문구처럼 보이게 임의로 고치지 않는다.

## 3. 색

각 색에는 프로젝트에서 사용할 의미 역할을 하나씩만 둔다. 상품 사진 내부 색과
ClearType 가장자리의 색 번짐은 팔레트로 채택하지 않는다.

| 토큰 | 색 | 역할 하나 | 읽은 위치 |
|---|---|---|---|
| `page-background` | `#F0F0F0` | 페이지 바깥 바탕 | `cart.png`의 `y=0–73`, `x=0–12` |
| `surface` | `#FFFFFF` | 기본 콘텐츠 바탕 | 세 화면의 헤더·본문·카드 내부 |
| `control-strip-surface` | `#FAFAFA` | 조작 항목을 모은 띠의 바탕 | `home.png` 정렬 바 `y=363–395` |
| `passive-surface` | `#F2F4F6` | 조작되지 않는 약한 면 | `cart.png` 비활성 버튼과 상품 행 사이 면 |
| `structure-border` | `#E0E3E7` | 큰 영역의 구조 경계 | `home.png` `y=105,135`; `detail.png` 옵션 행; `cart.png` 카드 |
| `list-border` | `#E5E7EB` | 상품 목록의 바깥 경계 | `cart.png` 상품 목록 `x=63/850`, `y=307` |
| `control-border` | `#C6CDD4` | 상태를 가진 조작 요소의 경계 | `detail.png` 탭 기준선, `cart.png` 수량 조절기 |
| `section-title-text` | `#000000` | 작은 섹션 제목 | `home.png`의 `식품`, `필터`, `카테고리` |
| `page-title-text` | `#131516` | 페이지 대제목 | `cart.png` `장바구니(9)`의 불투명 내부 픽셀 |
| `primary-text` | `#232B35` | 기본 본문 | `detail.png` 상품명·옵션, `cart.png` 상품명·요약 |
| `secondary-text` | `#667380` | 옵션·설명 보조문 | `detail.png` 원산지 설명, `cart.png` 옵션 |
| `tertiary-text` | `#787F83` | 가장 낮은 우선순위의 메타 정보 | `cart.png` 구매량·부가 정보 |
| `disabled-text` | `#ACB5BF` | 비활성 글자 | `cart.png` 비활성 단계와 주문 버튼 |
| `interaction-accent` | `#4269F6` | 조작 가능한 요소의 강조 | `home.png` 상단 분류 블록, `detail.png` 선택 탭·외곽 버튼 |
| `progress-current` | `#669DE0` | 주문 흐름의 현재 단계 번호 | `cart.png`의 `02` |
| `transaction-emphasis` | `#BA2D1B` | 거래에서 먼저 읽혀야 하는 정보 | `home.png`·`detail.png` 가격, `cart.png` 재고 경고 |
| `positive-status` | `#3D8A26` | 완료·긍정 상태 | `detail.png` 혜택 문구, `cart.png` 도착 상태 |

### 팔레트에서 제외한 색

- 원본 로고의 갈색·빨강·주황·파랑 계열은 원본 브랜드 색이므로 제외한다.
- `home.png`의 `#2E2E2E` 배너 배경과 배너 글자색은 배너 이미지에 포함된
  색이다. 진행 중인 프로모션이 없으므로 사용하지 않는다.
- 별점의 `#F19B38`, 적립 표시의 `#CCCCCC`·`#F7CB47`은 리뷰·적립 기능이
  없으므로 사용하지 않는다.
- 검색창 선 `#5383EC`은 상품 검색이 이번 범위에 없으므로 사용하지 않는다.
- `detail.png` 우측 고정 장바구니의 `#5F81F7`·`#EBF0FE`는 해당 기능을
  가져오지 않으므로 사용하지 않는다.
- WOW·로켓·배송 브랜드 배지에만 쓰인 색은 사용하지 않는다.

## 4. 글꼴과 글자 위계

### 사용자 승인 프로젝트 적용값 — 캡처 실측 아님

- 공급처: Google Fonts
- 프로젝트 적용 `font-family`: `"Noto Sans KR", sans-serif`
- CSS `font-size`: `12px`, `14px`, `16px`, `18px`, `20px`, `24px`, `32px`
- 숫자형 `font-weight`: `400`, `700`
- `line-height`: `normal`
- `letter-spacing`: `normal`
- 참고 화면의 정확한 글꼴은 래스터 이미지에서 식별하지 못했다. 위 글꼴과
  CSS 값은 사용자가 프로젝트 값으로 승인한 것이며 화면에서 읽은 값이 아니다.

### 화면에서 읽은 글자

`잉크 높이`는 글자 자체의 불투명 픽셀 높이다. CSS `font-size`나 행 상자의
높이가 아니다. 숫자형 굵기는 확정할 수 없어 보이는 굵기만 적는다.

| 화면과 요소 | 캡처 잉크 높이 | 화면에서 보이는 굵기 |
|---|---:|---|
| `home.png` 헤더 작은 글자 | 약 10–11px | 보통 |
| `home.png` 사이드바 제목 | 내부 12px, 전체 약 14px | 굵게 |
| `home.png` 사이드바 항목 | 약 8–10px | 보통 |
| `home.png` 목록 섹션 제목 | 내부 17px, 전체 약 19px | 굵게 |
| `home.png` 상품명 | 내부 10px, 전체 약 12px | 보통 |
| `home.png` 판매가 | 내부 약 10–11px, 전체 약 14px | 굵게 |
| `detail.png` 상품명 | 14px (`y=180–193`) | 보통 |
| `detail.png` 할인가 | 16px (`y=283–298`) | 굵게 |
| `detail.png` 탭·옵션 | 약 11–14px | 보통, 선택값은 굵게 |
| `detail.png` 외곽 버튼 | 약 11–14px | 보통 |
| `cart.png` 페이지 대제목 | 내부 40px, 전체 약 42px | 굵게 |
| `cart.png` 주문 단계 | 약 17–20px | 굵게 |
| `cart.png` 요약 제목 | 내부 17px, 전체 약 19px | 굵게 |
| `cart.png` 상품명 | 내부 14px, 전체 약 17px | 보통 |
| `cart.png` 큰 가격 | 내부 23px, 전체 약 25px | 굵게 |
| `cart.png` 가격의 `원` | 전체 약 16px | 보통 |
| `cart.png` 비활성 주문 버튼 | 내부 17px, 전체 약 19px | 굵게 |

## 5. 상품 목록 실측 — `home.png`

이미지 크기는 1106×797px이다.

| 항목 | 캡처 실측값 |
|---|---:|
| 헤더 | 높이 99px (`y=6–104`) |
| 상단 분류 블록 | 약 91×99px (`x=5–95`, `y=6–104`) |
| 경로 표시 영역 | 높이 29px, 위·아래 1px 선 |
| 왼쪽 영역 | 폭 166px (`x=5–170`) |
| 왼쪽 영역 오른쪽 경계 | `x=171`, 1px |
| 왼쪽 영역과 본문 사이 빈 폭 | 16px (`x=172–187`) |
| 본문 | 폭 900px (`x=188–1087`) |
| 본문 시작선에서 제목 잉크까지 | 23px |
| 참고 배너 | 900×146px (`y=183–328`) |
| 배너 아래 빈 행 | 34px |
| 정렬 바 | 900×33px (`y=363–395`) |
| 정렬 바 왼쪽 안쪽 | 약 9px |
| 정렬 항목 사이 세로선 | 각각 1×15px |
| 정렬 바 아래 상품 이미지까지 | 21px |
| 4열 중 한 열 | 225px |
| 세 번째 상품 사진의 보이는 사각 범위 | 약 181×182px |
| 체크박스 | 약 11×12px, 1px 회색선 |

- 프로모션 배너의 치수는 읽은 기록만 남기며 프로젝트 화면에는 배너를 만들지
  않는다.
- 상품 카드에는 외곽선, 그림자, 둥근 모서리가 보이지 않는다.
- 상품 이미지는 `SHOP.md`에 따라 1:1 정사각형 안에서 `object-fit: contain`으로
  보이고 자르지 않는다.
- 분류는 `SHOP.md`의 네 가지 값만 사용한다.

### 사용자 승인 프로젝트 적용값 — 캡처 실측 아님

- 목록과 상품 상세의 헤더 높이는 `99px`이다.
- 헤더에는 로고와 브랜드 이름을 넣지 않는다. 화면 이동은 `상품`, `장바구니`
  두 텍스트 링크만 사용한다.
- 목록 이미지 상자는 `180px × 180px`이고 `object-fit: contain`을 적용한다.
- 분류 띠는 `900px × 33px`이다. `전체`, `잡화`, `뷰티`, `신발`, `식품` 순서의
  다섯 조작을 각각 `180px` 너비로 한 줄에 배치한다.

## 6. 상품 상세 실측 — `detail.png`

이미지 크기는 1270×831px이다.

| 항목 | 캡처 실측값 |
|---|---:|
| 상단 분류 블록 | 약 91×99px (`x=9–99`, `y=2–100`) |
| 헤더 하단선 | `y=101`, 1px `structure-border` |
| 상품 정보 열 | 폭 548px (`x=573–1120`) |
| 탭 높이 | 약 30px |
| 탭 기준선 | 1px `control-border` (`y=443`) |
| 선택 탭 밑줄 | 147×3px (`x=847–993`, `y=440–442`) |
| 라디오 | 약 13×13px 원형 |
| 첫 옵션 행 | 약 52px |
| 이후 옵션 행 | 약 59–60px |
| 전체 폭 외곽 버튼 | 548×33px (`x=573–1120`, `y=715–747`) |

- 큰 상품 이미지 왼쪽, 이름·가격·설명·수량 선택은 오른쪽에 놓는 두 열 구조를
  따른다.
- 큰 상품 이미지 자체의 경계가 흰 바탕과 이어져 정확한 이미지 상자 크기는
  읽을 수 없었다.
- 옵션·정보 묶음은 카드 그림자 대신 1px 가로선으로 나눈다.
- 선택 탭만 파란 밑줄을 사용하고, 비선택 탭에는 회색 기준선만 둔다.
- 원형 찜·공유, 리뷰·구매자 수, 배송 보장, WOW 배지, 우측 고정 장바구니는
  구현하지 않는다.

### 사용자 승인 프로젝트 적용값 — 캡처 실측 아님

- 상품 상세의 헤더 높이는 `99px`이고 헤더 내용은 5절의 규칙을 따른다.
- 상세 이미지 상자는 `420px × 420px`이고 `object-fit: contain`을 적용한다.
- 활성 `장바구니 담기` 버튼은 `548px × 33px`이다. 배경은 `surface`, 글자는
  `interaction-accent`, 선은 1px `interaction-accent`, 모서리는 `0px`로 한다.

## 7. 장바구니 실측 — `cart.png`

이미지 크기는 1288×847px이다.

| 항목 | 캡처 실측값 |
|---|---:|
| 상단 회색 영역 | 높이 75px |
| 흰 본문 | 폭 1275px (`x=13–1287`) |
| 본문 안 콘텐츠 | 폭 1175px (`x=63–1237`) |
| 본문 좌우 여백 | 각 50px |
| 왼쪽 알림 카드 | 785×82px (`x=63–847`, `y=210–291`) |
| 오른쪽 요약 카드 | 375×288px (`x=863–1237`, `y=210–497`) |
| 알림 카드와 요약 카드 사이 | 15px |
| 알림 카드와 상품 목록 사이 | 15px |
| 알림 썸네일 | 60×60px |
| 알림 안쪽 왼쪽·위쪽 | 20px·10px |
| 알림 썸네일과 글자 사이 | 10px |
| 요약 카드 좌우 안쪽 | 25px |
| 비활성 주문 버튼 | 323×55px (`x=889–1211`, `y=422–476`) |
| 상품 사진 | 약 175×175px |
| 체크박스 | 30×30px |
| 수량 조절기 | 140×40px |
| 상품 행 사이 구분 면 | 10px (`y=607–616`) |

- 상품 행은 캡처 기준으로 `왼쪽 안쪽 20 → 체크박스 30 → 빈칸 25 → 이미지
  175 → 빈칸 10 → 상품 정보` 순서다.
- 선택 체크박스와 품절 임박 알림은 `SHOP.md`에 해당 기능이나 데이터가 없으므로
  구현하지 않는다. 위 치수는 화면을 읽은 기록으로만 남긴다.
- 삭제는 별도 장식 버튼보다 밑줄이 있는 작은 텍스트 동작으로 보인다.
- 상품 사진은 1:1 영역에서 `contain`으로 표시한다.
- 배송비·쿠폰·할인 행은 만들지 않는다.

### 사용자 승인 프로젝트 적용값 — 캡처 실측 아님

- 장바구니와 주문 완료의 헤더 높이는 `75px`이고 헤더 내용은 5절의 규칙을
  따른다.
- 활성 `주문하기` 버튼은 `323px × 55px`이다. 배경은 `surface`, 글자는
  `interaction-accent`, 선은 1px `interaction-accent`로 한다. 모서리는
  `cart.png`에서 읽은 주문 버튼의 약 `4px`를 따른다.

## 8. 선과 모서리

아래 값도 CSS를 확인한 값이 아니라 캡처에서 보이는 픽셀값이다.

| 대상 | 캡처에서 보이는 선 | 캡처에서 보이는 모서리 |
|---|---|---:|
| 상품 목록 카드 | 없음 | 0px |
| 상품 목록 정렬 바 | 외곽선 없음 | 0px |
| 상세 탭 기준선 | 1px `control-border` | 0px |
| 상세 선택 탭 | 3px `interaction-accent` | 0px |
| 상세 외곽 버튼 | 1px `interaction-accent` | 0px |
| 장바구니 알림 카드 | 1px `structure-border` | 약 5px |
| 장바구니 요약 카드 | 1px `structure-border` | 약 5px |
| 장바구니 상품 목록 | 1px `list-border` | 약 5px |
| 장바구니 수량 조절기 | 1px `control-border` | 약 5px |
| 장바구니 비활성 버튼 | 선 없음 | 약 4px |
| 상품 행 사이 구분 면 | 10px `passive-surface` | 0px |

- 기본 그림자는 보이지 않는다.
- `detail.png`의 펼쳐진 원본 카테고리 패널에만 약 3px의 회색 페이드가
  보이지만, 프로젝트에는 메가 메뉴가 없으므로 그림자 값은 정의하지 않는다.
- 원형은 라디오처럼 형태상 원이어야 하는 조작에만 쓴다. 일반 버튼과 카드에
  pill 모서리를 사용하지 않는다.

## 9. 조작 요소 상태

활성 `장바구니 담기`와 `주문하기` 버튼은 세 화면에 보이지 않는다. 아래 활성
상태는 캡처 실측이 아니라 사용자가 승인한 프로젝트 적용값이다.

| 요소 | 크기 | 배경 | 글자 | 선 | 모서리 | 근거 |
|---|---|---|---|---|---|---|
| 상세 외곽 조작 | 캡처 548×33px | `surface` | `interaction-accent` | 1px `interaction-accent` | 캡처 0px | 캡처 실측 |
| 주문 버튼 비활성 | 캡처 323×55px | `passive-surface` | `disabled-text` | 없음 | 캡처 약 4px | 캡처 실측 |
| 장바구니 담기 활성 | CSS 548×33px | `surface` | `interaction-accent` | 1px `interaction-accent` | 0px | 사용자 승인 |
| 주문하기 활성 | CSS 323×55px | `surface` | `interaction-accent` | 1px `interaction-accent` | 캡처 약 4px | 색·상태는 사용자 승인, 모서리는 캡처 실측 |

장바구니가 비었을 때만 비활성 스타일을 사용한다. 활성 버튼의 빈칸을 다른
화면이나 관습적인 색으로 임의 보충하지 않는다.

모든 키보드 조작 요소의 초점 표시는 `3px solid #4269F6` 외곽선으로 한다.
이는 캡처 실측이 아니라 사용자가 승인한 프로젝트 접근성 값이다.

## 10. 참고 화면에서 가져오지 않는 것

- 원본 사이트 로고, 브랜드 이름, 서비스 아이콘과 문구
- 참고 화면 속 상품 사진, 상품명, 브랜드, 가격, 홍보 문구
- 프로모션 배너와 캐러셀
- 브랜드·가격대·멤버십 필터
- 네 분류보다 큰 메가 메뉴
- 별점, 리뷰 수, 구매자 수
- 무료배송, 도착 보장, 배송 브랜드, 적립 배지
- 실제 데이터가 없는 품절 임박 수량
- 찜, 공유, 우측 고정 미니 장바구니

제외한 자리는 장식이나 가짜 정보로 채우지 않는다. 프로젝트 상품과 기능은
항상 `SHOP.md`를 따른다.

## 11. 화면별 근거 위치 요약

- `home.png`: `#E0E3E7`은 전체 너비 `y=105,135`; 본문은
  `x=188–1087`; 정렬 바는 `y=363–395`; 4열은 각각 225px로 읽었다.
- `detail.png`: 상품 정보 열은 `x=573–1120`; 선택 탭 파란선은
  `x=847–993, y=440–442`; 외곽 버튼은 `x=573–1120, y=715–747`에서
  읽었다.
- `cart.png`: 본문 콘텐츠는 `x=63–1237`; 알림 카드는 `x=63–847`;
  요약 카드는 `x=863–1237`; 상품 행 구분 면은 `y=607–616`에서 읽었다.
- `Noto Sans KR`, CSS 글자 크기·굵기, 목록·상세 이미지 상자, 분류 띠,
  헤더, 활성 담기·주문 버튼, 키보드 초점 값은 화면에서 읽은 값이 아니라
  사용자가 승인한 프로젝트 적용값이다.
